
{# On hérite de squelette.jinja #}

{% extends "squelette.jinja"%}


{# Dans lequel on remplace le block main par ceci: #}


{% block main %}

        <h1>Liste des classe</h1>

        <a href = "{{ url_for('classe_bp.create_classe') }}">Ajouter une nouvelle classes</a>

        
        <div
            class="table"
        >
            <table
                class="table table-primary table-striped table-hover"
            >
                <thead>
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">Nom</th>
                        <th scope="col">eleves</th>
                    </tr>
                </thead>
                <tbody>
                    {# Ci dessous, on boucle sur la liste d'élèves qui a été transmise par le contrôleur.
                    Un navigateur ne sait pas interpréter ce code. #}
                    {% for classe in classe %}
                        <tr class="">
                            <td scope="row"> 
                                <a href="{{ url_for('classe_bp.read_classe', id=classe.id) }}">{{ classe.id }}</a>
                            </td>
                            <td>{{ classe.nom }}</td>
                            <td>{{ classe.eleves }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        </main>

{% endblock %}